@model IEnumerable<eform.Models.vwPrjCode>

@{
    Layout = "~/Views/Shared/_PrjLayout.cshtml";

}

<div class="section">
    <div class="row col-md-12">
        <div class="box">
            <div class="box-header text-center">
                <h4>專案討論區成員設定</h4>
            </div>
            <div class="box-header">
                
            </div>
            <div class="box-body">

                <table class="table table-bordered table-condensed">
                    <tr>
                        <th style="width:100px;"></th>
                        <th>
                            @Html.DisplayNameFor(model => model.code)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.nm)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.owner)
                        </th>
                    </tr>

                    @foreach (var item in Model) {
            <tr>
                <td>
                    <a class="btn btn-primary" href="@Url.Action("ForumUserList","Prj",new { id = item.id })"><span class="fa fa-users">論壇成員</span></a>
                </td>

            <td>
                @Html.DisplayFor(modelItem => item.code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.owner)
            </td>
        </tr>
    }

                </table>

            </div>
        </div>
    </div>
</div>